VITE_API_URL=http://localhost:8001/api
# Optional: set this only if you want the frontend to call the HX Engine directly
# (bypasses the Vite proxy). Leave unset to use the Vite proxy (recommended for dev).
# VITE_HX_ENGINE_URL=http://localhost:8100
